# Telegram bot framework
aiogram>=3.13,<4.0

# HTTP server for webhooks / healthcheck
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27
# Обязателен для request.form(): Mango шлёт webhook-и form-data. Без этого пакета
# Starlette роняет ЛЮБОЙ разбор формы (даже urlencoded) — webhook-и молча не работают.
python-multipart>=0.0.9

# Database
SQLAlchemy[asyncio]>=2.0,<2.1
asyncpg>=0.29
alembic>=1.13

# HTTP client for amoCRM / Mango
aiohttp>=3.9

# Configuration
pydantic>=2.5
pydantic-settings>=2.1
